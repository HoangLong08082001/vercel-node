function validateForm() {
  const username = document.getElementById("username").value;
  const password = document.getElementById("password").value;
  const urlParams = new URLSearchParams(window.location.search);
  const ref = urlParams.get("ref");

  const validateEmail = (email) => {
    const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return re.test(String(email).toLowerCase());
  };

  const validatePassword = (password) => {
    const re = /^(?=.*[A-Z])(?=.*[a-z])(?=.*\d)[A-Za-z\d@$!%*?&]{8,}$/;
    return re.test(password);
  };

  if (username.trim() === "") {
    alert("Username không được để trống!");
    return false;
  }
  if (!validateEmail(username)) {
    alert("Email không đúng định dạng");
    return false;
  }

  if (password.trim() === "") {
    alert("Password không được để trống!");
    return false;
  }
  if (!validatePassword(password)) {
    alert(
      "Mật khẩu không đủ mạnh. Yêu cầu mật khẩu tối thiểu 8 ký tự, có ít nhất 1 chữ hoa, 1 chữ thường, và 1 con số"
    );
    return false;
  }

  return true;
}
// window.onload = function () {
//   // Lấy URL hiện tại
//   const urlParams = new URLSearchParams(window.location.search);

//   // Kiểm tra sự tồn tại của tham số 'ref'
//   if (!urlParams.has("ref")) {
//     // Nếu không có tham số 'ref', chuyển hướng hoặc hiển thị thông báo
//     window.location.href = "https://example.com/error"; // Chuyển hướng đến trang lỗi hoặc trang chủ
//   }
// };
